{
  "name": "safe-top-level-await",
  "version": "2.0.0",
  "description": "Use `await` at your code's top-level (without IIFE)",
  "main": "index.js",
  "repository": "uwx-node-modules/safe-top-level-await",
  "author": "uwx, Robert Klep <robert@klep.name>",
  "engines": {
    "node": ">=7.6.0"
  },
  "scripts": {
    "postinstall": "node check-version.js"
  },
  "license": "MIT",
  "dependencies": {
    "semver": "^5.3.0"
  }
}
